# CLI Description Review

Source: MetaDocs suite workspace only.

Collected from `DocumentationSubject` where `Kind=CliApplication` and current app-level `DocumentationNarrative` rows. Command-level descriptions, option descriptions, README files, COMMANDS files, and rendered HTML were intentionally not scraped in this pass.

## meta

Product group: meta
Source/project: ..\meta\Meta\Cli
Subject key if available: source:cli:meta:app

Current rendered description:
```text
`meta` is the core workspace CLI for creating, inspecting, editing, comparing, and maintaining modeled metadata.
```

CLI definition description:
```text
`meta` is the core workspace CLI for creating, inspecting, editing, comparing, and maintaining modeled metadata.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta` is the core workspace CLI for creating, inspecting, editing, comparing, and maintaining modeled metadata.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta` is the core workspace CLI for creating, inspecting, editing, comparing, and maintaining modeled metadata.
```

Notes:
duplicate descriptions

## meta-docs

Product group: meta
Source/project: ..\meta\MetaDocs\Cli
Subject key if available: source:cli:meta-docs:app

Current rendered description:
```text
`meta-docs` turns CLI definitions, workspace models, selected instance facts, and authored narrative into refreshable reference documentation.
```

CLI definition description:
```text
`meta-docs` turns CLI definitions, workspace models, selected instance facts, and authored narrative into refreshable reference documentation.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-docs:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-docs` turns CLI definitions, workspace models, selected instance facts, and authored narrative into refreshable reference documentation.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-docs` turns CLI definitions, workspace models, selected instance facts, and authored narrative into refreshable reference documentation.
```

Notes:
duplicate descriptions

## meta-weave

Product group: meta
Source/project: ..\meta\MetaWeave\Cli
Subject key if available: source:cli:meta-weave:app

Current rendered description:
```text
`meta-weave` creates MetaWeave workspaces for explicit model-to-model weaving, where structure is carried across modeled surfaces instead of hidden in ad hoc conversion code.
```

CLI definition description:
```text
`meta-weave` creates MetaWeave workspaces for explicit model-to-model weaving, where structure is carried across modeled surfaces instead of hidden in ad hoc conversion code.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-weave:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-weave` creates MetaWeave workspaces for explicit model-to-model weaving, where structure is carried across modeled surfaces instead of hidden in ad hoc conversion code.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-weave` creates MetaWeave workspaces for explicit model-to-model weaving, where structure is carried across modeled surfaces instead of hidden in ad hoc conversion code.
```

Notes:
duplicate descriptions

## meta-schema

Product group: meta-bi
Source/project: MetaSchema\Cli
Subject key if available: source:cli:meta-schema:app

Current rendered description:
```text
`meta-schema` extracts live SQL Server schema structure into sanctioned MetaSchema workspaces, making database objects available as explicit metadata.
```

CLI definition description:
```text
`meta-schema` extracts live SQL Server schema structure into sanctioned MetaSchema workspaces, making database objects available as explicit metadata.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-schema:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-schema` extracts live SQL Server schema structure into sanctioned MetaSchema workspaces, making database objects available as explicit metadata.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-schema` extracts live SQL Server schema structure into sanctioned MetaSchema workspaces, making database objects available as explicit metadata.
```

Notes:
duplicate descriptions

## meta-data-type

Product group: meta-bi
Source/project: MetaDataType\Cli
Subject key if available: source:cli:meta-data-type:app

Current rendered description:
```text
`meta-data-type` bootstraps sanctioned MetaDataType workspaces that define the data-type vocabulary used across Meta-BI models and conversions.
```

CLI definition description:
```text
`meta-data-type` bootstraps sanctioned MetaDataType workspaces that define the data-type vocabulary used across Meta-BI models and conversions.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-data-type:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-data-type` bootstraps sanctioned MetaDataType workspaces that define the data-type vocabulary used across Meta-BI models and conversions.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-data-type` bootstraps sanctioned MetaDataType workspaces that define the data-type vocabulary used across Meta-BI models and conversions.
```

Notes:
duplicate descriptions

## meta-data-type-conversion

Product group: meta-bi
Source/project: MetaDataTypeConversion\Cli
Subject key if available: source:cli:meta-data-type-conversion:app

Current rendered description:
```text
`meta-data-type-conversion` authors and validates sanctioned conversion graphs between data types, including resolution from a source type through modeled conversion rules.
```

CLI definition description:
```text
`meta-data-type-conversion` authors and validates sanctioned conversion graphs between data types, including resolution from a source type through modeled conversion rules.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-data-type-conversion:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-data-type-conversion` authors and validates sanctioned conversion graphs between data types, including resolution from a source type through modeled conversion rules.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-data-type-conversion` authors and validates sanctioned conversion graphs between data types, including resolution from a source type through modeled conversion rules.
```

Notes:
duplicate descriptions

## meta-convert

Product group: meta-bi
Source/project: MetaConvert\Cli
Subject key if available: source:cli:meta-convert:app

Current rendered description:
```text
`meta-convert` performs explicit cross-model conversions between supported Meta-BI workspaces, carrying structure from one modeled surface into another.
```

CLI definition description:
```text
`meta-convert` performs explicit cross-model conversions between supported Meta-BI workspaces, carrying structure from one modeled surface into another.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-convert:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-convert` performs explicit cross-model conversions between supported Meta-BI workspaces, carrying structure from one modeled surface into another.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-convert` performs explicit cross-model conversions between supported Meta-BI workspaces, carrying structure from one modeled surface into another.
```

Notes:
duplicate descriptions

## meta-sql

Product group: meta-bi
Source/project: MetaSql\Cli
Subject key if available: source:cli:meta-sql:app

Current rendered description:
```text
`meta-sql` plans and applies manifest-driven SQL Server deployments from sanctioned MetaSql workspaces.
```

CLI definition description:
```text
`meta-sql` plans and applies manifest-driven SQL Server deployments from sanctioned MetaSql workspaces.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-sql:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-sql` plans and applies manifest-driven SQL Server deployments from sanctioned MetaSql workspaces.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-sql` plans and applies manifest-driven SQL Server deployments from sanctioned MetaSql workspaces.
```

Notes:
duplicate descriptions

## meta-transform-script

Product group: meta-bi
Source/project: MetaTransform\Script\Cli
Subject key if available: source:cli:meta-transform-script:app

Current rendered description:
```text
`meta-transform-script` models a bounded SQL transform surface as semantically round-trippable workspace data, preserving transform intent through import, emit, and diff cycles.
```

CLI definition description:
```text
`meta-transform-script` models a bounded SQL transform surface as semantically round-trippable workspace data, preserving transform intent through import, emit, and diff cycles.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-transform-script:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-transform-script` models a bounded SQL transform surface as semantically round-trippable workspace data, preserving transform intent through import, emit, and diff cycles.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-transform-script` models a bounded SQL transform surface as semantically round-trippable workspace data, preserving transform intent through import, emit, and diff cycles.
```

Notes:
duplicate descriptions

## meta-transform-binding

Product group: meta-bi
Source/project: MetaTransform\Binding\Cli; MetaTransform\Binding\CliDefinition
Subject key if available: source:cli:meta-transform-binding:app

Current rendered description:
```text
`meta-transform-binding` binds MetaTransformScript workspaces to explicit source and target schema contracts, producing rowset, column, and validation evidence.
```

CLI definition description:
```text
`meta-transform-binding` binds MetaTransformScript workspaces to explicit source and target schema contracts, producing rowset, column, and validation evidence.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-transform-binding:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-transform-binding` binds MetaTransformScript workspaces to explicit source and target schema contracts, producing rowset, column, and validation evidence.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-transform-binding` binds MetaTransformScript workspaces to explicit source and target schema contracts, producing rowset, column, and validation evidence.
```

Notes:
duplicate descriptions

## meta-data-quality

Product group: meta-bi
Source/project: MetaDataQuality\Cli
Subject key if available: source:cli:meta-data-quality:app

Current rendered description:
```text
`meta-data-quality` derives reviewable MetaDataQuality candidates from the typed semantic graph of MetaTransformScript workspaces and promotes them into executable quality assets.
```

CLI definition description:
```text
`meta-data-quality` derives reviewable MetaDataQuality candidates from the typed semantic graph of MetaTransformScript workspaces and promotes them into executable quality assets.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-data-quality:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-data-quality` derives reviewable MetaDataQuality candidates from the typed semantic graph of MetaTransformScript workspaces and promotes them into executable quality assets.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-data-quality` derives reviewable MetaDataQuality candidates from the typed semantic graph of MetaTransformScript workspaces and promotes them into executable quality assets.
```

Notes:
duplicate descriptions

## meta-data-warehouse

Product group: meta-bi
Source/project: MetaDataWarehouse\Cli
Subject key if available: source:cli:meta-data-warehouse:app

Current rendered description:
```text
`meta-data-warehouse` models logical dimensional warehouse concepts, separating warehouse intent from SQL Server realization and deployment mechanics.
```

CLI definition description:
```text
`meta-data-warehouse` models logical dimensional warehouse concepts, separating warehouse intent from SQL Server realization and deployment mechanics.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-data-warehouse:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-data-warehouse` models logical dimensional warehouse concepts, separating warehouse intent from SQL Server realization and deployment mechanics.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-data-warehouse` models logical dimensional warehouse concepts, separating warehouse intent from SQL Server realization and deployment mechanics.
```

Notes:
duplicate descriptions

## meta-datavault-raw

Product group: meta-bi
Source/project: MetaDataVault\Cli\Raw
Subject key if available: source:cli:meta-datavault-raw:app

Current rendered description:
```text
`meta-datavault-raw` creates sanctioned raw Data Vault workspaces for modeling source-oriented Data Vault structures.
```

CLI definition description:
```text
`meta-datavault-raw` creates sanctioned raw Data Vault workspaces for modeling source-oriented Data Vault structures.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-datavault-raw:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-datavault-raw` creates sanctioned raw Data Vault workspaces for modeling source-oriented Data Vault structures.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-datavault-raw` creates sanctioned raw Data Vault workspaces for modeling source-oriented Data Vault structures.
```

Notes:
duplicate descriptions

## meta-datavault-business

Product group: meta-bi
Source/project: MetaDataVault\Cli\Business
Subject key if available: source:cli:meta-datavault-business:app

Current rendered description:
```text
`meta-datavault-business` creates sanctioned business Data Vault workspaces for modeling business-oriented vault structures.
```

CLI definition description:
```text
`meta-datavault-business` creates sanctioned business Data Vault workspaces for modeling business-oriented vault structures.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-datavault-business:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-datavault-business` creates sanctioned business Data Vault workspaces for modeling business-oriented vault structures.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-datavault-business` creates sanctioned business Data Vault workspaces for modeling business-oriented vault structures.
```

Notes:
duplicate descriptions

## meta-pipeline

Product group: meta-bi
Source/project: MetaPipeline\Cli
Subject key if available: source:cli:meta-pipeline:app

Current rendered description:
```text
`meta-pipeline` creates sanctioned MetaPipeline workspaces that describe transform steps and pipeline structure as explicit metadata.
```

CLI definition description:
```text
`meta-pipeline` creates sanctioned MetaPipeline workspaces that describe transform steps and pipeline structure as explicit metadata.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-pipeline:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-pipeline` creates sanctioned MetaPipeline workspaces that describe transform steps and pipeline structure as explicit metadata.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-pipeline` creates sanctioned MetaPipeline workspaces that describe transform steps and pipeline structure as explicit metadata.
```

Notes:
duplicate descriptions

## meta-orchestration

Product group: meta-bi
Source/project: MetaOrchestration\Cli
Subject key if available: source:cli:meta-orchestration:app

Current rendered description:
```text
`meta-orchestration` creates MetaOrchestration workspaces from modeled pipeline and binding metadata, turning execution structure into inspectable orchestration data.
```

CLI definition description:
```text
`meta-orchestration` creates MetaOrchestration workspaces from modeled pipeline and binding metadata, turning execution structure into inspectable orchestration data.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-orchestration:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-orchestration` creates MetaOrchestration workspaces from modeled pipeline and binding metadata, turning execution structure into inspectable orchestration data.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-orchestration` creates MetaOrchestration workspaces from modeled pipeline and binding metadata, turning execution structure into inspectable orchestration data.
```

Notes:
duplicate descriptions

## meta-tabular

Product group: meta-bi
Source/project: MetaTabular\Cli
Subject key if available: source:cli:meta-tabular:app

Current rendered description:
```text
`meta-tabular` creates sanctioned MetaTabular workspaces for modeling tabular analytics artifacts.
```

CLI definition description:
```text
`meta-tabular` creates sanctioned MetaTabular workspaces for modeling tabular analytics artifacts.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-tabular:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-tabular` creates sanctioned MetaTabular workspaces for modeling tabular analytics artifacts.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-tabular` creates sanctioned MetaTabular workspaces for modeling tabular analytics artifacts.
```

Notes:
duplicate descriptions

## meta-multi-dimensional

Product group: meta-bi
Source/project: MetaMultiDimensional\Cli
Subject key if available: source:cli:meta-multi-dimensional:app

Current rendered description:
```text
`meta-multi-dimensional` creates sanctioned MetaMultiDimensional workspaces for modeling multidimensional analytics artifacts.
```

CLI definition description:
```text
`meta-multi-dimensional` creates sanctioned MetaMultiDimensional workspaces for modeling multidimensional analytics artifacts.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-multi-dimensional:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-multi-dimensional` creates sanctioned MetaMultiDimensional workspaces for modeling multidimensional analytics artifacts.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-multi-dimensional` creates sanctioned MetaMultiDimensional workspaces for modeling multidimensional analytics artifacts.
```

Notes:
duplicate descriptions

## meta-analytics

Product group: meta-bi
Source/project: MetaAnalytics\Cli
Subject key if available: source:cli:meta-analytics:app

Current rendered description:
```text
`meta-analytics` owns common analytics concepts that can be realized through target-specific tabular or multidimensional models.
```

CLI definition description:
```text
`meta-analytics` owns common analytics concepts that can be realized through target-specific tabular or multidimensional models.
```

README description candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

COMMANDS/docs candidates:
```text
not collected; latest instruction limited this pass to the MetaDocs workspace
```

MetaDocs narrative candidates:
- Id: source:cli:meta-analytics:app:narrative:summary:010
  Slot: Summary
  Origin: Authored
  Body:
```text
`meta-analytics` owns common analytics concepts that can be realized through target-specific tabular or multidimensional models.
```

Other candidate fragments:
- MetaDocs workspace DocumentationSubject.Summary
```text
`meta-analytics` owns common analytics concepts that can be realized through target-specific tabular or multidimensional models.
```

Notes:
duplicate descriptions

