<?xml version="1.0" encoding="utf-8"?>
<MetaDocs>
  <DocumentationNarrativeList>
    <DocumentationNarrative Id="public:meta-bi:cli:narrative:summary:cli" DocumentationSubjectId="public:meta-bi:cli">
      <Body>Command-line tools for BI modeling, conversion, validation, and execution.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Summary</Slot>
      <Title>CLI</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta-bi:models:narrative:summary:models" DocumentationSubjectId="public:meta-bi:models">
      <Body>Model references for the sanctioned BI metadata workspaces.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Summary</Slot>
      <Title>Models</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta-bi:overview:narrative:summary:overview" DocumentationSubjectId="public:meta-bi:overview">
      <Body>`meta-bi` is the BI stack that sits on top of the generic `meta` foundation.

It is not only a set of code generators. The important product shape is a modeled BI stack: source contracts, transform syntax, binding, target deployment assets, data quality, pipeline execution, orchestration, and analytical targets remain explicit workspaces that can be inspected, replayed, and validated.

Two high-value behaviors are intentionally model-driven:

- `meta-data-quality` automatically derives reviewable DQ candidates from the modeled transform graph. Supported checks come from `MetaTransformScript` structure and optional binding evidence, not from hand-authored one-off test SQL.
- `meta-orchestration` automatically infers the safe part of the run graph from modeled `MetaPipeline` tasks and bound transform access profiles. When write ordering or synchronization cannot be proven, it records modeled policy issues instead of hiding uncertainty in scheduler code.

For the normal modeled BI path -- source schema, SQL transforms in the supported `MetaTransformScript` surface, strict binding, generated DQ, modeled pipeline tasks, orchestration, and Tabular/MultiDimensional output -- this covers a large automatic slice of the stack. Dynamic SQL or legacy opaque procedural SQL needs explicit guarantees before orchestration/DQ can safely trust it.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Summary</Slot>
      <Title>Overview</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:cli:narrative:summary:cli" DocumentationSubjectId="public:meta:cli">
      <Body>Command-line tools for the core metadata foundation.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Summary</Slot>
      <Title>CLI</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:models:narrative:summary:models" DocumentationSubjectId="public:meta:models">
      <Body>Model references for the core metadata foundation.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Summary</Slot>
      <Title>Models</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:cliataglance:meta-cli-at-a-glance" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:workspacecontract:workspace-contract">
      <Body>`meta` operates on workspaces and provides these capability groups:

Workspace operations: create and inspect workspaces (`init`, `status`).
Validation and inspection: check integrity and explore model/instance (`check`, `list`, `view`, `query`, `graph`).
Edits: mutate models and instance data (`model ...`, `insert`, `delete`, `bulk-insert`, `instance update`, `instance rename-id`, `instance relationship set|list`, `instance diff`, `instance merge`).
Model analysis and guided refactor: read-only relationship inference (`model suggest`) and atomic model+instance refactors (`model refactor property-to-relationship`, `model refactor relationship-to-property`).
Pipelines: import and emit representations (`import ...`, `generate ...`).

Workflow: model + instance workspace -&gt; `meta` emits C#/SQL consumables -&gt; your patterns/generators produce organization-specific artifacts.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>CliAtAGlance</Slot>
      <Title>Meta CLI at a glance</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:commandguide:command-guide" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:installandrun:install-and-run">
      <Body>Workspace discovery defaults to the current directory (searching upward). Use `--workspace &lt;path&gt;` to override.

Global behavior:

- successful state changes answer with `Ok`.
- validation/usage/runtime failures explain what cannot continue and exit non-zero.
- `--strict` upgrades warnings to errors for mutating commands.

Command discovery:

- `meta help`: show top-level command groups and examples.
- `meta &lt;command&gt; help`: show usage/options/examples for one command.
- `meta &lt;command&gt; &lt;subcommand&gt; --help`: show detailed subcommand help.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>CommandGuide</Slot>
      <Title>Command guide</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:foundations:metadata-foundations" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:overview:deterministic-metadata-backend">
      <Body>In this project, metadata is not "extra comments about data". Metadata is the product model itself:

- `model.xml`: the schema contract (entities, scalar properties, required relationships).
- `instances/*.xml`: the instance graph for that model (rows + relationship usages).
- `workspace.xml`: workspace-level configuration for layout/encoding/order and storage.

Core terms:

- `Model`: named set of entity definitions.
- `Entity`: a record type (similar to a table).
- `Property`: scalar attribute on an entity instance.
- `Relationship`: required reference from one entity to another.
- `Instance`: one row/record of an entity, identified by `Id`.
- `Workspace`: on-disk unit that contains model + instance + workspace config.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Foundations</Slot>
      <Title>Metadata foundations</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:installandrun:install-and-run" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:repositoryclis:repository-clis">
      <Body>Build:

`dotnet build Metadata.Framework.sln`
`dotnet build MetaWeave.sln`

On Windows, these builds refresh the published framework-dependent CLIs at:

- `Meta\Cli\bin\publish\win-x64\meta.exe`
- `MetaWeave\Cli\bin\publish\win-x64\meta-weave.exe`
- `MetaDocs\Cli\bin\publish\win-x64\meta-docs.exe`

Run directly:

`dotnet run --project Meta/Cli/Meta.Cli.csproj -- help`

If `Meta\Cli\bin\publish\win-x64` is on your `PATH`:

`meta help`

Or run the executable directly:

`Meta\Cli\bin\publish\win-x64\meta.exe help`

Build the installer package:

`powershell -NoProfile -ExecutionPolicy Bypass -File Meta\Installer\package-offline.ps1`

The default package is framework-dependent and shared-DLL based, so it requires the .NET 8 runtime on the target machine. Use `-SelfContained` only when you need a larger package that carries the runtime.

Then install the foundation CLIs (`meta`, `meta-weave`, `meta-docs`) into `%LOCALAPPDATA%\meta\bin` and add that directory to your user `PATH`:

`Meta\Installer\bin\publish\win-x64-framework-dependent-shared\install-meta.exe`</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>InstallAndRun</Slot>
      <Title>Install and run</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:instancexmlcontract:instance-xml-contract" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:modelxmlcontract:model-xml-contract">
      <Body>`instances/*.xml` stores the data for a model.

The root element is the model name (for example `&lt;EnterpriseBIPlatform&gt;`).

Each entity's instances are grouped under a deterministic list container element (for example `&lt;CubeList&gt;`, `&lt;MeasureList&gt;`). Inside the container, each record is written as the singular entity element (for example `&lt;Cube ...&gt;`, `&lt;Measure ...&gt;`).

Every record must have an `Id="..."` attribute.

Relationships are stored as `...Id` attributes on the record. By default a relationship to `Cube` is stored as `CubeId="..."`. If the relationship has a role like `role="SourceCube"`, it is stored as `SourceCubeId="..."`.

Scalar properties are written as child elements. Missing element means unset. An empty element means explicit empty string.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>InstanceXmlContract</Slot>
      <Title>Instance XML contract</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:internalpackages:internal-packages" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:commandguide:command-guide">
      <Body>The foundation libraries are packable as internal NuGet packages:

- `Meta.Core`
- `Meta.Adapters`
- `MetaWeave.Core`

Pack them into the local feed at `.nupkg` with:

`pack-internal.cmd`

Downstream repositories should consume these packages instead of reaching back into this repo with project references. That keeps the foundation boundary explicit and prevents BI-side drift from silently editing core.

Today `meta-bi` consumes `Meta.Core` from this feed. `MetaWeave.Core` is published alongside it so downstream repos can adopt weave core package references without reopening the source boundary.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>InternalPackages</Slot>
      <Title>Internal packages</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:isomorphism:why-isomorphic-metadata" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:foundations:metadata-foundations">
      <Body>Isomorphic means the same semantics can live in multiple forms without translation drift.

Think of it as one model with three native surfaces, each optimized for a different kind of work:

- XML workspace: canonical, deterministic, and git-friendly (clean diffs, reviewable history, mergeable refactors).
- SQL: data-first and database-native (easy to inspect/query/validate at scale; good interchange format when your source of truth starts in a database).
- C#: application-first (strongly typed objects for tools and apps; no XML parsing in consumer code; easy to integrate into pipelines/services).

Because the semantics are the same, multiple producers and consumers can collaborate without rewriting the model for each layer:

- analysts/modelers edit model + instance in workspace form,
- platform engineers consume SQL outputs and SQL-project consumables in their pipelines,
- application/tooling engineers consume emitted C# APIs.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Isomorphism</Slot>
      <Title>Why isomorphic metadata</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:modelxmlcontract:model-xml-contract" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:cliataglance:meta-cli-at-a-glance">
      <Body>`model.xml` defines the schema for your metadata workspace: which entities exist, what properties they have, and how entities relate to each other.

A model has one root `&lt;Model name="..."&gt;` and then:

- `&lt;Entity&gt;` defines a record type (like a table).
- `name="Cube"` is the singular name.
- Instances are grouped under a deterministic `&lt;EntityName&gt;List` container in XML and emitted C# root collections use the same `&lt;EntityName&gt;List` naming.
- `&lt;PropertyList&gt;` lists scalar fields for the entity. `dataType="string"` is the default and omitted; properties are required by default (`isRequired="true"`), and optional fields use `isRequired="false"`.
- `&lt;RelationshipList&gt;` lists required foreign-key style references to other entities. A relationship points to a target entity and is required by default. In instance XML it becomes `${TargetEntity}Id` by default. If you need multiple relationships to the same target, specify `role="..."` and it becomes `${Role}Id`.
- `Id` is implicit on every entity, so `Property name="Id"` is not written.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>ModelXmlContract</Slot>
      <Title>Model XML contract</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:overview:deterministic-metadata-backend" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:summary:overview">
      <Body>`meta` is a deterministic metadata backend. The canonical representation is an XML workspace on disk (git-friendly), but you can round-trip: materialize a workspace from SQL, emit SQL/C# representations and SQL-project consumables, and load/save model instances via C# consumables for tooling.

BI-specific sanctioned models and CLIs live in the separate `meta-bi` repository.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Overview</Slot>
      <Title>Deterministic metadata backend</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:repositoryclis:repository-clis" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:instancexmlcontract:instance-xml-contract">
      <Body>This repo contains these CLI projects:

- `meta` (Meta CLI): workspace/model/instance operations, diff/merge, import, generate.
- `meta-weave` (MetaWeave CLI): authoring, suggestion, validation, and materialization of sanctioned cross-model property bindings.
- `meta-docs` (MetaDocs CLI): modeled CLI documentation import and merged metametabi-style docs rendering.
- `meta-mesh` (MetaMesh CLI): declared workspace sets and executable operations over those workspaces.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>RepositoryClis</Slot>
      <Title>Repository CLIs</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:summary:overview" DocumentationSubjectId="public:meta:overview">
      <Body>Start here for the model-first metadata foundation behind meta and meta-bi.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Summary</Slot>
      <Title>Overview</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:versioncontrol:why-git-matters-for-metadata" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:isomorphism:why-isomorphic-metadata">
      <Body>The workspace is designed for version control:

- deterministic writer output (stable ordering, utf-8 no bom, LF) keeps diffs clean.
- model and instance changes are code-reviewed like source code.
- branch/merge workflows apply to metadata evolution.
- refactors are auditable through before/after XML deltas.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>VersionControl</Slot>
      <Title>Why git matters for metadata</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:meta:overview:narrative:workspacecontract:workspace-contract" DocumentationSubjectId="public:meta:overview" PreviousNarrativeId="public:meta:overview:narrative:versioncontrol:why-git-matters-for-metadata">
      <Body>A workspace is a directory containing:

`workspace.xml`
`model.xml`
`instances/...`

Instance data may be sharded: multiple instance files can contain rows for the same entity; load merges those shards and save preserves existing shard file layout (new rows for an entity are written to that entity's primary shard).</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>WorkspaceContract</Slot>
      <Title>Workspace contract</Title>
    </DocumentationNarrative>
    <DocumentationNarrative Id="public:reference:narrative:summary:meta-meta-bi-reference" DocumentationSubjectId="public:reference">
      <Body>Command-line and model references for the current public MetaDocs suite.</Body>
      <BodyFormat>PlainText</BodyFormat>
      <Origin>Authored</Origin>
      <ReviewStatus>Current</ReviewStatus>
      <Slot>Summary</Slot>
      <Title>meta + meta-bi reference</Title>
    </DocumentationNarrative>
  </DocumentationNarrativeList>
</MetaDocs>